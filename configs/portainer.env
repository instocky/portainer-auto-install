# Переменные окружения для Portainer
# Копировать в .env для использования с docker-compose

# Основные настройки
PORTAINER_VERSION=latest
PORTAINER_LOG_LEVEL=INFO

# Порты (можно изменить если нужны другие)
PORTAINER_HTTP_PORT=9000
PORTAINER_HTTPS_PORT=9443
PORTAINER_EDGE_PORT=8000

# Пути для volume (по умолчанию используются Docker volumes)
# PORTAINER_DATA_PATH=/opt/portainer/data

# SSL настройки (для production)
# PORTAINER_SSL_CERT=/path/to/cert.pem
# PORTAINER_SSL_KEY=/path/to/key.pem

# Настройки базы данных (для внешней БД)
# PORTAINER_DATABASE_URL=postgres://user:pass@host:port/db

# Настройки аутентификации
# PORTAINER_ADMIN_PASSWORD_FILE=/run/secrets/portainer_admin_password

# Дополнительные флаги
# PORTAINER_FLAGS=--hide-label owner=portainer
